import { IconButtonTemplateProps } from "./iconButtonTemplate";

const copy: IconButtonTemplateProps = {
	type: 'copy',
	onClick: () => console.log('copy'),
};

const mockIconButtonTemplateProps = {
	copy,
};

export default mockIconButtonTemplateProps;
